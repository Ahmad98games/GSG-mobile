/**
 * DEPRECATED: This file was intended for Next.js Edge Functions.
 * It is not compatible with the React Native mobile environment.
 * (c) 2026 Gold She Industrial ERP
 */

/*
import { NextRequest, NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';
import { Tier, requiresTier } from './TierGuard';

export async function verifyTier(req: NextRequest, required: Tier) {
  // Logic removed for mobile compatibility
}
*/
export const verifyTierPlaceholder = true;
